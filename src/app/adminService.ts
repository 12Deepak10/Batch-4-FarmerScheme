import { HttpClient } from "@angular/common/http";
import { Observable } from "rxjs";

export class AdminService
{
    constructor(private http:HttpClient)
    {

    }
 //   getBidder(id:number):Observable<BidderDto
}